<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />2<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />0<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />1<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />5<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />-<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />M<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />A<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />S<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />L<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />E<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />N<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />S<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />E<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />S<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />-<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />W<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />S<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />W<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />G<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />E<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />E<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />A<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />S<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />_<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />A<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />U<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />P<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />|<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />|<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />|<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />|<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />|<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />P<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />?<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />A<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />|<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />[<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />]<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />S<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />P<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />M<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />M<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />F<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />U<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />F<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />U<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />W<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />W<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />S<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />P<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />A<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />|<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />[<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />]<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />[<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />]<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />X<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />X<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />Y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />|<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />|<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />0<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />&<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />&<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />-<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />1<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />?<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />-<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />1<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />Y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />|<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />|<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />0<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />&<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />&<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />-<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />1<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />?<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />-<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />1<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />A<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />X<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />Y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />X<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />Y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />A<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />U<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />G<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />M<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />M<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />M<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />W<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />A<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />P<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />|<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />[<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />]<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />A<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />|<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />[<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />]<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />S<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />W<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />A<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />W<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />A<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />P<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />[<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />]<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />S<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />W<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />G<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />[<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />2<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />2<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />]<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />S<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />W<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />G<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />A<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />A<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />N<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />`<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />$<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />?<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />`<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />9<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />4<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />%<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />1<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />0<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />L<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />4<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />8<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />1<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />[<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />]<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />*<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />1<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />0<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />0<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />-<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />1<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />&<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />&<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />2<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />N<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />`<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />$<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />2<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />?<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />`<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />9<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />4<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />%<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />1<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />0<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />L<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />4<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />8<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />N<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />`<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />$<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />?<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />`<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />9<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />4<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />%<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />1<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />0<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />L<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />4<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />8<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />-<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />1<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />&<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />&<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />2<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />N<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />`<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />$<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />O<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />2<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />?<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />`<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />9<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />4<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />%<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />1<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />0<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />L<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />4<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />8<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />:<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />P<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />I<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />N<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />-<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />F<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />W<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />A<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />0<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />&<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />&<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />A<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />S<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />F<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />N<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />-<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />N<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />-<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />N<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />-<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />b<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />-<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />g<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />!<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />y<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />&<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />&<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />M<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />w<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />E<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />h<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />A<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />'<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />=<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />{<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />T<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />B<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />k<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />,<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />/<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />v<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />}<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />x<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />p<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />f<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />u<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /> <RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />R<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />c<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />.<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />m<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />(<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />r<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />d<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />D<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />a<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />i<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />l<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />C<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />o<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />e<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />n<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />t<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />s<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />)<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      /><RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
<RichTextEditor
                          id={id}
                          text=""
                          placeholderText="Add a description..."
                          onBlur={(text) => {
                              if (text) {
                                  addTextBlock(card, intl, text)
                              }
                          }}
                          onEditorCancel={() => setShowEmptyPlaceholder(false)}
                          autofocus={!card.id || card.id === "templates"}
                      />
